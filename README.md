一个基于c++14标准写的双人对战象棋小程序。
实现了象棋规则约束移动、悔棋、保存与回放（可以指定回放跳到哪一步）、自定义棋子颜色、自定义倒计时、鼠标点击移动的功能。
单击选择时有高亮提示，再次点击确认操作。右键取消选择。
程序采用光标定位实现局部刷新绘制图案，避免频繁刷新导致屏幕闪烁。但是此操作存在覆盖不完全，存在残影的bug未被修复。
另外由于本人大一写该程序时只学了c++语法未学数据结构与算法课程，很多内容都是边学边做，水平有限，导致程序算法可能较为低下。
最后，本程序使用<windows.h>库，因此在c++17标准下会存在宏污染导致程序报错。
